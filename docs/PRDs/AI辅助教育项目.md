# AI 辅助教育项目 PRD

## 目标

为个人学习者建立可恢复、可验证的学习资料与计划流水线。

## 首期范围

当前注册的 skills 负责格式转换、英语写作支持和学习流程辅助；各 skill 的输入、输出和运行状态必须按自身契约分离保存。

## 非目标

首期不自动决定考试、不替用户生成承诺性的成绩预测、不上传个人资料，也不接入外部模型或第三方服务。

## 验收

- 各已注册 skill 的入口命令能够生成其定义的正式产物、状态文件和事件文件。
- `verify` 能独立发现缺失、格式错误和产物缺失。
- 状态和正式产物位于规范目录，临时和日志文件不进入 Git。
