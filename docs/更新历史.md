# 更新历史

## 当前工作区变更

- 将原学习者档案初始化 skill 移入 `skills/.backup/`，并从插件清单及活动文档中取消注册。
- 完善 `en-writing-master` 的 EPUB 写作范围状态机、翻译章节排除和来源筛选审计。
- 将语言库抽取固化到共享 Python 脚本，保留完整去重条目、真实使用案例和来源定位。
- 扩展工具 schema、Markdown 模板和验证门禁，并重新生成 `cet4-writing`。

## 0.1.0

- 建立脚本优先、状态机驱动的 Agent 项目模板。
- 默认包含 Git 差异、敏感信息检查和格式转换 skills。

## 0.1.0（项目初始化）

- 按 Agent 开源项目编写规范建立项目骨架。
- 增加 `init` 学习者档案 skill、状态检查点、Schema 和测试。
- 保留仓库原有 CC BY-NC 许可证。
